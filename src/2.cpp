#include <iostream>
using namespace std;

int main() {
    // Ask user for their name
    cout << "What is your name?" << endl;
    string name;
    cin >> name;

    // Greet the user by name
    cout << "Hello, " << name << "!" << endl;

    // Print a message to the user
    cout << "Welcome to the school project!" << endl;

    return 0;
}
